package mid.utility ver 1;
import up.*;
import java.util.*;

/*
public class ExtendedPoint extends Point {
  public ExtendedPoint(float x, float y) {
    super(x,y);
  }
  public double distanceFromZero() {
    return new Point(0f,0f).distance(new Point(0f,0f), new Point(0f,0f));
  }
  public Point parent() {
    return new Point(getX(), getY());
  }

  public Point recursive() {
    return recursive();
  }
}
*/

public class ExtendedPoint extends Point {
  public ExtendedPoint(float initX, float initY) {
    super(initX,initY);
  }


  public Point method() {
    return new ExtendedPoint(0,0);
  }


  public Point recursive() {
    return recursive();
  }
  /*
  public static void main(String[] args) {
    Point p1 = new ExtendedPoint(0,1f);
    p1 = new Point(0,0);

    //double t = p1.distance(p1);
    //p1.y = 2;
  }
  */

}